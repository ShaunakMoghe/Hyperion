# Copy to .env for local runs. Never commit .env (see .gitignore).
# No live credentials here: Stripe paths accept test keys only (sk_test_/rk_test_).
POSTGRES_USER=hyperion
POSTGRES_PASSWORD=changeme
POSTGRES_DB=hyperion
DATABASE_URL=postgresql://hyperion:changeme@localhost:5432/hyperion
HYPERION_LLM_BUDGET_USD=20
HYPERION_LLM_PROVIDER=
HYPERION_LLM_MODEL=
STRIPE_TEST_KEY=
