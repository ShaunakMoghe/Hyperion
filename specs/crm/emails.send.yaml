spec_version: 1
id: crm.emails.send
system: crm
operation: {method: POST, path: "/emails"}
effect_class: irreversible # the stored row IS the delivery; nothing un-sends it
fidelity: none
before_image: null
produces:
  - {name: email_id, from: "$.response.id"}
inverse: null
verify:
  # No verification is performed for irreversible calls (fidelity none);
  # the read below only records post-state for the ledger.
  read: {method: GET, path: "/snapshot", params: {}}
  compare: {fields: [], against: before_image}
provenance: {source: human, model: null, verified: false, trials: 0, reviewed_by: null}
